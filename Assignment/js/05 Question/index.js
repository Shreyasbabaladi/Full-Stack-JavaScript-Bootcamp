// 05. Declare a varibale and assign string value to it and then split it into an array using split() method

let something = 'Something to work on the string';

const stringSpliter = something => something.split(' ');

console.log(stringSpliter(something));