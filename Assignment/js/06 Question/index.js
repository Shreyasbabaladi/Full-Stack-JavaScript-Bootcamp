// 06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.
let compys = 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon';

const stringArray = words => words.split(", ");

console.log(stringArray(compys));